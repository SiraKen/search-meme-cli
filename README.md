# Search Your Meme on "Know Your Meme"

## Build and installation

Run the following command:

```bash
go build -o meme
sudo ./install
```

To uninstall, run the command below:

```bash
sudo ./uninstall
```
